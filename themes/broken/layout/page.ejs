<% if (is_page() && page.type === 'about') { %>
	<%- partial('_partial/about') %>
<% } %>

<% if (is_page() && page.type === 'links') { %>
	<%- partial('_partial/links') %>
<% } %>

<% if (is_page() && page.type === 'author') { %>
	<%- partial('_partial/author') %>
<% } %>

<% if (is_page() && page.type === '404') { %>
	<%- partial('_partial/404') %>
<% } %>

<% if (is_page() && page.type === 'search') { %>
	<%- partial('_partial/search') %>
<% } %>